let index = 0

while (index <= 10) {
    // console.log(`The value is : ${index}`);
    index = index +2
}

let arr = 0
let myArray = ["aaditya", "vaasu", "prashant", "baadal"]
while (arr < myArray.length) {
    // console.log(`Value of arr is ${myArray[arr]}`);
    arr = arr +1
}

let score = 11
do {
    console.log(`The value of score is ${score}`);
    // score = score+1
    score++
} while (score <= 10);